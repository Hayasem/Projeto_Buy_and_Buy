<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tela_Login_ProjetoIntegrador"
        tools:targetApi="31">
        <activity
            android:name=".BottomSheetProdutoActivity"
            android:exported="false" />
        <activity
            android:name=".activitys.activity_recuperar_senha"
            android:exported="false" />
        <activity
            android:name=".fragment.CartScreen"
            android:exported="false" />
        <activity
            android:name=".activitys.MenuScreen"
            android:exported="false" />
        <activity
            android:name=".activitys.TelaPerfilUsuario"
            android:exported="false" />
        <activity
            android:name=".activitys.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity -->
        <!-- android:name=".TelaPerfilUsuario" -->
        <!-- android:exported="true" -->
        <!-- android:launchMode="singleTop" -->
        <!-- /> -->
        <activity
            android:name=".activitys.FormCadastro"
            android:exported="true"
            android:launchMode="singleTop" />
        <activity
            android:name=".activitys.SecondActivity"
            android:exported="true"
            android:launchMode="singleTop" />
    </application>

</manifest>